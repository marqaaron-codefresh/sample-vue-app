(function (window) {
    window.__env = window.__env || {};
    // eslint-disable-next-line no-constant-condition
    window.__env.VERSION = "" ? "" : "local" ;
    // eslint-disable-next-line no-constant-condition
    window.__env.WEB_SERVER = "" ? "" : "locally" ;
}(this));